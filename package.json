{
  "name": "ml-catalog-assistant",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web-app",
    "dev:api": "npm run dev --workspace=api",
    "dev:ml": "npm run dev --workspace=ml-service"
  }
}
